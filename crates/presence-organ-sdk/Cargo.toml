[package]
name = "presence-organ-sdk"
version = "0.1.0"
edition = "2021"
description = "Universal Rust SDK for Presence sensory and action organs"
license = "MIT"
authors = ["Presence Contributors"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"