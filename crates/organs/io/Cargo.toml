[package]
name = "organ-io"
version = "0.3.0"
edition = "2021"
authors = ["hrkcz001"]
license = "Apache-2.0"
description = "Native pure-Rust physical input/output and filesystem manipulation organ for Presence"

[dependencies]
presence-organ-sdk = { path = "../../presence-organ-sdk" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
