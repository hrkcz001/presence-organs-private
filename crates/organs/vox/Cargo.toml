[package]
name = "organ-vox"
version = "0.1.0"
edition = "2021"
description = "Native pure-Rust voice and audio capture organ for Presence"
license = "MIT"

[dependencies]
presence-organ-sdk = { path = "../../presence-organ-sdk", version = "0.1.0" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"